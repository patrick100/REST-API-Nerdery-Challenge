{
  "watch": "src",
  "ext": "ts, js, json, yaml",
  "ignore": ["node_modules", "coverage", "dist"],
  "exec": "ts-node ./src/server.ts",
  "restartable": "rs",
  "env": {
    "NODE_ENV": "development"
  }
}
