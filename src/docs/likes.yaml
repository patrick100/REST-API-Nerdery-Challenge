paths:
  /accounts/{accountId}/posts/{postId}/like:
    post:
      security:
        - jwt: []
      tags:
        - Likes
      summary: Give a 'like' from a post
      produces:
        - application/json
    delete:
      security:
        - jwt: []
      tags:
        - Likes
      summary: Remove a 'like' from a post
      produces:
        - application/json

  /accounts/{accountId}/posts/{postId}/dislike:
    post:
      security:
        - jwt: []
      tags:
        - Likes
      summary: Give a 'dislike' from a post
      produces:
        - application/json
    delete:
      security:
        - jwt: []
      tags:
        - Likes
      summary: Remove a 'dislike' from a post
      produces:
        - application/json

  /accounts/{accountId}/comments/{commentId}/like:
    post:
      security:
        - jwt: []
      tags:
        - Likes
      summary: Give a 'like' from a comment
      produces:
        - application/json
    delete:
      security:
        - jwt: []
      tags:
        - Likes
      summary: Remove a 'like' from a comment
      produces:
        - application/json

  /accounts/{accountId}/comments/{commentId}/dislike:
    post:
      security:
        - jwt: []
      tags:
        - Likes
      summary: Give a 'dislike' from a comment
      produces:
        - application/json
    delete:
      security:
        - jwt: []
      tags:
        - Likes
      summary: Remove a 'dislike' from a comment
      produces:
        - application/json
